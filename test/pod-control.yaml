apiVersion: v1
kind: Pod
metadata:
  generateName: test-pod-control-
  labels:
    k8s-cluster-managers-tests: "true"
spec:
  restartPolicy: "Never"
  containers:
  - name: sleep
    image: alpine
    command: ["sleep"]
    args: ["60"]
